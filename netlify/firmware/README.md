# Fichiers de test OTA

La phase 3.0.1 publie ici deux fichiers texte inertes. Ils servent uniquement a
tester le telechargement en flux, la taille, SHA-256 et le nettoyage des `.new`.
Ils ne sont jamais importes ou executes par le programme.
